# Twitcher — web

The Next.js frontend. See the [project README](../README.md) for the full architecture, tech stack, and setup instructions covering both `web/` and `api/`.

```sh
cp .env.example .env.local   # fill in Clerk keys + NEXT_PUBLIC_API_BASE_URL
npm install
npm run dev   # http://localhost:3000
```
